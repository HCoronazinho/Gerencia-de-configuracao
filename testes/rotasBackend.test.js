const test = require("node:test");
const assert = require("node:assert");

test("rota existe", () => {
  assert.ok(true);
});

test("get lancamentos", () => {
  assert.ok(true);
});

test("post lancamentos", () => {
  assert.ok(true);
});